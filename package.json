{
	"name": "monorepo",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "npm run build --prefix apps/web",
		"deploy": "gh-pages -d apps/web/dist"
	},
	"devDependencies": {
		"concurrently": "^9.1.2",
		"gh-pages": "^6.3.0"
	},
	"workspaces": [
		"apps/*"
	]
}
